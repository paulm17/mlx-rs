[server]
bind = "127.0.0.1"
port = 8080
# model_path = "/path/to/model.gguf"
# rate_limit_rpm = 120
# api_key is loaded from .env file (HF_API_KEY)
logging = ""

# llama.cpp engine options
n_ctx = 4096
# Maximum concurrent generation sequences. Keep at 1 unless the context is
# deliberately sized for parallel sequences (each sequence gets less context).
n_seq_max = 1
n_batch = 512
n_ubatch = 512
# n_gpu_layers = 99       # layers offloaded to GPU/Metal
# n_threads = 4           # CPU threads for generation
# n_threads_batch = 4     # CPU threads for batch processing
embedding = true
pooling = "cls"
# Maximum number of input texts accepted per HTTP embedding request. This is
# independent of n_seq_max and does not reduce generation context capacity.
embeddings_batch_size = 32
# use_mmap = true         # memory-map model file
# use_mlock = false       # lock model in RAM
# flash_attn = false      # flash attention

# MLX engine options
# mlx_cache_limit = 64        # max prefix cache entries
# mlx_compile = true          # enable MLX compilation
# mlx_prefill_chunk_size = 2048  # prefill chunk size in tokens
